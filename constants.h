#pragma once

#include <math.h>
#include <stdlib.h>

/* Mathematical constants */
extern const double PI;

/* Program parameters */
// default string size for names of files, directories and commands
extern const int STR_SIZE;

// minimum possible number of the dipoles in a shape
extern const size_t MIN_DIPOLE_NUMBER;
// maximum possible coordinate of a dipole
extern const double MAX_COORD;
// assumed accuracy of the dipole axis beta angle calculation
extern const size_t DIVISION_NUMBER;

extern const double EPS;